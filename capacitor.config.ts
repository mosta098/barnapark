import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.BarnaParking',
  appName: 'BarnaParking',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
